import { PlayeNamePipe } from './playe-name.pipe';

describe('PlayeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PlayeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
